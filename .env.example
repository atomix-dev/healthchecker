# Comma-separated list of URLs to check
TARGET_URLS=https://example.com/protected,https://another-service.com/api/v1/data

# Email Configuration for Nodemailer
# You can use services like Gmail, Outlook, etc.
# For Gmail, you might need to use an "App Password"
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-email-password-or-app-password
EMAIL_RECIPIENT=recipient-email@example.com

# --- Cron & Environment Configuration ---
# Set to 'development' for more frequent checks (5 seconds default), or 'production' for hourly checks.
NODE_ENV=development

# Optional: Uncomment to manually set a specific cron schedule.
# This will override the default based on NODE_ENV.
# e.g., '*/10 * * * * *' for every 10 seconds.
# CRON_SCHEDULE=*/5 * * * * *
